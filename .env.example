# Finalyst Static Demo Configuration
# Copy this file to .env and modify as needed

# Invite code required to access the demo (shared via URL param)
INVITE_CODE=your-invite-code

# Password required to login (entered in login form)
PASSWORD=your-secret-password

# Server port (default: 80 for production)
PORT=80
