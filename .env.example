# Finalyst Static Demo Configuration
# Copy this file to .env and modify as needed

# Server port (default: 80 for production, 3001 for dev)
PORT=80

# Data directory path (default: ./data relative to server)
# Can be absolute path or relative to server location
# DATA_DIR=./data
# DATA_DIR=/path/to/external/data

# ============================================
# MULTI-CLIENT MULTI-VERSION ARCHITECTURE
# ============================================
#
# Credentials are stored in client.json files inside DATA_DIR
#
# Data Structure:
#   {DATA_DIR}/
#   ├── pipeline-config.json          # Shared pipeline config
#   ├── fyst/                          # Client folder
#   │   ├── client.json                # Client credentials & config
#   │   ├── mainline/                  # Version folder
#   │   │   ├── version.json           # Version metadata
#   │   │   ├── merchants.json         # Static data files...
#   │   │   └── ...
#   │   └── demo-1/                    # Another version
#   │       ├── version.json
#   │       └── ...
#   └── sends/                         # Another client
#       ├── client.json
#       └── mainline/
#           └── ...
#
# client.json format:
#   {
#     "id": "fyst",
#     "name": "Fyst Analytics",
#     "invite_code": "fyst-Xk9mP2nQr5tY",
#     "password": "fyst-Hw3jL8vBn4kZ",
#     "default_version": "mainline",
#     "versions": ["mainline", "demo-1"]
#   }
#
# version.json format:
#   {
#     "id": "mainline",
#     "name": "Mainline",
#     "description": "Production configuration"
#   }
#
# Access URLs:
#   - Fyst: http://localhost?invite=fyst-Xk9mP2nQr5tY
#   - Sends: http://localhost?invite=sends-Qw7eR3tYu9iO
#
# Reload data without restart:
#   curl -X POST http://localhost/api/reload
#
# Version switching:
#   Use the version selector in the sidebar (only shows if client has multiple versions)
